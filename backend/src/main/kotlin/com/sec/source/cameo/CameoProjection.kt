package com.sec.source.cameo

// Cameo-specific read projections (SOI views, functions). Reuses the source-agnostic
// Tier-1 vocabulary (__child, __sortKey) rather than inventing its own (CLAUDE.md R3).
public object CameoProjection
