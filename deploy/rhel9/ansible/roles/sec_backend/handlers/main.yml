---
- name: Restart the backend
  ansible.builtin.systemd_service:
    name: sec-backend
    state: restarted
    daemon_reload: true

- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true
