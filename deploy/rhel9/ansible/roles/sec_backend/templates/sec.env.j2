# MANAGED BY ANSIBLE — edits here are overwritten on the next run.
# Change a value in the vault instead:  ansible-vault edit group_vars/vault.yml
#
# Read by systemd as pid 1 through EnvironmentFile=, before it drops to User=sec.

SEC_NEO4J_USER={{ vault_sec_neo4j_user }}
SEC_NEO4J_PASSWORD={{ vault_sec_neo4j_password }}
SEC_OIDC_CLIENT_SECRET={{ vault_sec_oidc_client_secret }}
SEC_JIRA_TOKEN={{ vault_sec_jira_token | default('') }}
