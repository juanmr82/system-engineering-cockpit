---
- name: Restart keycloak
  ansible.builtin.systemd_service:
    name: sec-keycloak
    state: restarted
    daemon_reload: true

- name: Reload systemd for keycloak
  ansible.builtin.systemd_service:
    daemon_reload: true
